# Development dependencies.
#
# Kept as a convenience installer; it mirrors [project.optional-dependencies]
# dev in pyproject.toml, which is the source of truth. Prefer:
#   pip install -e ".[dev]"
#
# The runtime deps come in via -r, so they are declared in exactly one place.
-r requirements.txt
pytest>=8,<10
pip-audit>=2.7
